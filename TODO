- Clean up shutdown to not try to avoid erroring, and not report expected
  errors.

- Add some reasonably long and configurable absolute limits to prevent node
  failures from hanging forever.

- State indicators
  - getAvailableServers()
  - getUnavailableServers()

- JSON Transcoder
